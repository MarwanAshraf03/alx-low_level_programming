#include "main.h"

/**
 * _isupper - detects if the character is upper case or not
 * @c: the the character needed to observe
 *
 * Return: returns 0 if the char is lowercase and 1 if char is uppercase
 */

int _isupper(int c)
{
	return (c >= 65 && c <= 90);
}
